services:
  twitch-farm:
    build: .
    container_name: twitch-farm
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
      - ./cookies:/app/cookies
    environment:
      - TZ=UTC

